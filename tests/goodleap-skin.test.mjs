import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const css=fs.readFileSync(new URL("../app/goodleap/goodleap.css",import.meta.url),"utf8");test("GoodLeap uses current green cream yellow skin",()=>{for(const token of ["#123d31","#f6f1e7","#f5c542"])assert.ok(css.includes(token),token)});test("GoodLeap mobile rules exist",()=>assert.ok(css.includes("@media(max-width:760px)")));
